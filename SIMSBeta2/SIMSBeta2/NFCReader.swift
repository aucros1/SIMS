//
//  NFCReader.swift
//  SIMSBeta2
//
//  Created by Alvaro Ucros on 11/18/22.
//
/*
import IdentivNFCFramework
import UIKit

class NFCReader: UIViewController, IdentivNFCDelegate {

    override func viewDidLoad() {
        super.viewDidLoad()

        let tag = IdentivNFCTag()
        tag.delegate = self
        tag.tagType = .TYPE2
        tag.newNDEFReaderSession()
        tag.readNDEF()
        // Do any additional setup after loading the view.
    }
    

    func didConnectTag(tagType: NFCTagType) {
        print(tagType)
    }
    func didReadNDEFMessage(message: NFCNDEFMessage?) {
        print(message)
    }
    /*
    // MARK: - Navigation

    // In a storyboard-based application, you will often want to do a little preparation before navigation
    override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
        // Get the new view controller using segue.destination.
        // Pass the selected object to the new view controller.
    }
    */

}
*/
